name: DevOps CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  validate-and-test:
    name: Build & Test Microservices
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout codebase
      - name: Checkout Code
        uses: actions/checkout@v4

      # 2. Set up Node.js Environment
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: |
            frontend/package-lock.json
            node-service/package-lock.json

      # 3. Build React Frontend
      - name: Build React Frontend
        run: |
          cd frontend
          if [ ! -f package-lock.json ]; then npm i --package-lock-only; fi
          npm ci
          npm run build

      # 4. Test Node Service
      - name: Test Node Service
        run: |
          cd node-service
          if [ ! -f package-lock.json ]; then npm i --package-lock-only; fi
          npm ci
          npm test

      # 5. Set up Java Environment
      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      # 6. Build Java Spring Boot Service
      - name: Build Java Spring Boot Service
        run: |
          cd java-service
          mvn clean package -B

      # 7. Validate Docker Compose Syntax
      - name: Docker Compose Validation
        run: |
          cp .env.example .env
          docker compose config

      # 8. Verify Individual Docker Container Builds
      - name: Docker Build Validation
        run: |
          echo "Building Frontend container..."
          docker build -t devops-frontend:ci ./frontend

          echo "Building Node Service container..."
          docker build -t devops-node-service:ci ./node-service

          echo "Building Java Service container..."
          docker build -t devops-java-service:ci ./java-service

          echo "All container builds validated successfully!"
